name = "studycloud-worker"
main = "src/index.ts"
compatibility_date = "2024-03-01"

# ==============================================================================
# Domaine Personnalisé Cloudflare
# ==============================================================================
routes = [
  { pattern = "api-worker.dkd-technologies.com", custom_domain = true }
]

# ==============================================================================
# Liaison Base de Données Cloudflare D1
# ==============================================================================
[[d1_databases]]
binding = "DB"
database_name = "studycloud-db"
database_id = "VOTRE_DATABASE_ID_ICI" # Remplacez par l'ID fourni par wrangler d1 create

# ==============================================================================
# Liaison Bucket de Stockage Cloudflare R2
# ==============================================================================
[[r2_buckets]]
binding = "BUCKET"
bucket_name = "studycloud-files"
