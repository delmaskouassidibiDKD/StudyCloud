{
  "name": "studycloud-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker backend pour StudyCloud (D1 SQL + R2 Storage)",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "build": "wrangler deploy -c wrangler.toml --dry-run --outdir=dist && node build-dist.cjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {},
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240208.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.30.0"
  }
}
